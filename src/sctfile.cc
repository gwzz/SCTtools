#include "sctfile.h"
#include <string>
#include <sstream>
#include <vector>


// void Menu::readFile() {
//     string line;
//     vector <string> lines;
//     ifstream myfile ("file.txt");

//     if (myfile.is_open()) {
//         while (getline (myfile, line)) {
//           cout << line << "\n";
//           lines.push_back(line);
//         }

//         myfile.close();
//     } else { 
//        cout << "Unable to open file"; 
//     }
// }